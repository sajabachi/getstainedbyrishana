import React, { useState, useEffect, useRef } from 'react';
import { 
  PenTool, 
  Wind, 
  Award, 
  ChevronRight, 
  Instagram, 
  Mail, 
  Phone,
  Menu,
  X,
  MousePointer2,
  Sparkles,
  ShieldCheck,
  Timer,
  ArrowDown
} from 'lucide-react';

const App = () => {
  const [isScrolled, setIsScrolled] = useState(false);
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  
  // Intersection Observer for Scroll Reveals
  useEffect(() => {
    const handleScroll = () => setIsScrolled(window.scrollY > 50);
    window.addEventListener('scroll', handleScroll);

    const observerOptions = { threshold: 0.15 };
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('reveal-visible');
        }
      });
    }, observerOptions);

    document.querySelectorAll('.reveal-on-scroll').forEach(el => observer.observe(el));
    
    return () => {
      window.removeEventListener('scroll', handleScroll);
      observer.disconnect();
    };
  }, []);

  const services = [
    {
      title: "Live Calligraphy",
      subtitle: "Bespoke Engraving & Gifting",
      description: "Speed & Precision: 45-60 seconds per personalized item. Optimized for luxury fragrance brands.",
      icon: <PenTool className="w-6 h-6" />,
      tag: "Corporate Ready"
    },
    {
      title: "Bespoke Henna",
      subtitle: "Heritage Meets Modernity",
      description: "100% organic materials with a signature sensory scent. A ritual of elegance.",
      icon: <Wind className="w-6 h-6" />,
      tag: "Sensory Stain"
    },
    {
      title: "The Artist's Edit",
      subtitle: "Curated Experiences",
      description: "Exclusive art services designed to elevate boutique presence and VIP engagement.",
      icon: <Award className="w-6 h-6" />,
      tag: "Premium"
    }
  ];

  return (
    <div className="min-h-screen bg-[#FDFBF7] text-[#1A1A1A] font-sans selection:bg-[#D4AF37] selection:text-white overflow-x-hidden">
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Bodoni+Moda:ital,opsz,wght@0,6..96,400;0,6..96,700;1,6..96,400&family=Inter:wght@300;400;500;600&display=swap');
        
        body { font-family: 'Inter', sans-serif; }
        .font-serif { font-family: 'Bodoni Moda', serif; }

        .reveal-on-scroll {
          opacity: 0;
          transform: translateY(30px);
          transition: all 0.8s cubic-bezier(0.22, 1, 0.36, 1);
        }
        .reveal-visible {
          opacity: 1;
          transform: translateY(0);
        }
        
        .floating-asset {
          animation: float 6s ease-in-out infinite;
        }
        @keyframes float {
          0%, 100% { transform: translateY(0) rotate(0deg); }
          50% { transform: translateY(-20px) rotate(2deg); }
        }

        .glass-card {
          background: rgba(255, 255, 255, 0.4);
          backdrop-filter: blur(8px);
          border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .stagger-1 { transition-delay: 0.1s; }
        .stagger-2 { transition-delay: 0.2s; }
        .stagger-3 { transition-delay: 0.3s; }
      `}</style>

      {/* Navigation */}
      <nav className={`fixed w-full z-[100] transition-all duration-700 ${isScrolled ? 'bg-white/80 backdrop-blur-xl py-3 shadow-sm' : 'bg-transparent py-8'}`}>
        <div className="max-w-7xl mx-auto px-6 flex justify-between items-center">
          <div className="group cursor-pointer">
            <div className="flex flex-col">
              <span className="text-xl md:text-2xl font-serif tracking-[0.2em] uppercase transition-transform duration-500 group-hover:scale-105">Get Stained</span>
              <span className="text-[9px] tracking-[0.4em] uppercase opacity-60 text-[#D4AF37]">by Rishana</span>
            </div>
          </div>
          
          <div className="hidden md:flex space-x-10 text-[10px] tracking-[0.25em] uppercase font-semibold">
            {['The Artist', 'The Edit', 'Portfolio', 'Contact'].map((item, i) => (
              <a key={i} href={`#${item.toLowerCase().replace(' ', '')}`} className="relative py-2 hover:text-[#D4AF37] transition-colors group">
                {item}
                <span className="absolute bottom-0 left-0 w-0 h-[1px] bg-[#D4AF37] transition-all duration-300 group-hover:w-full"></span>
              </a>
            ))}
          </div>

          <button onClick={() => setIsMenuOpen(true)} className="md:hidden p-2">
            <Menu className="w-6 h-6" />
          </button>
        </div>
      </nav>

      {/* Hero Section */}
      <header className="relative h-screen flex items-center justify-center bg-[#FDFBF7] overflow-hidden">
        <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[90vw] h-[90vw] md:w-[50vw] md:h-[50vw] opacity-[0.03] floating-asset pointer-events-none z-0">
          <svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg" className="w-full h-full fill-[#1A1A1A]">
            <path d="M44.7,-76.4C58.1,-69.2,69.5,-57.4,77.3,-43.8C85.1,-30.2,89.2,-15.1,88.4,-0.4C87.7,14.2,82.1,28.4,73.8,40.8C65.5,53.2,54.4,63.7,41.4,71.4C28.4,79.1,14.2,84,0.4,83.3C-13.4,82.6,-26.8,76.3,-39.1,68.1C-51.4,59.9,-62.6,49.8,-70.7,37.5C-78.8,25.2,-83.8,10.6,-83.5,-3.8C-83.2,-18.2,-77.6,-32.4,-68.3,-43.9C-59,-55.4,-46.1,-64.2,-32.7,-71.4C-19.3,-78.6,-5.4,-84.2,9.3,-84.2C24.1,-84.2,31.3,-83.6,44.7,-76.4Z" transform="translate(100 100)" />
          </svg>
        </div>

        <div className="relative z-10 text-center px-6 max-w-5xl">
          <div className="reveal-on-scroll reveal-visible mb-6 inline-flex items-center gap-2 px-3 py-1 rounded-full border border-stone-200 bg-white/50 backdrop-blur-sm">
            <Sparkles className="w-3 h-3 text-[#D4AF37]" />
            <span className="text-[9px] tracking-[0.3em] uppercase font-bold opacity-60">The Art of Lasting Impressions</span>
          </div>
          
          <h1 className="text-5xl md:text-9xl font-serif mb-10 leading-[0.9] tracking-tight reveal-on-scroll reveal-visible stagger-1">
            Heritage <br />
            <span className="italic font-normal text-stone-400">meets</span> <br />
            Precision.
          </h1>
          
          <div className="flex flex-col md:flex-row items-center justify-center gap-6 mt-16 reveal-on-scroll reveal-visible stagger-2">
            <button className="group relative overflow-hidden bg-[#1A1A1A] text-white px-12 py-5 text-xs tracking-[0.3em] uppercase transition-all duration-500 hover:bg-[#D4AF37] hover:shadow-2xl hover:shadow-[#D4AF37]/20 w-full md:w-auto">
              <span className="relative z-10">The Collection</span>
            </button>
            <button className="group flex items-center gap-3 border border-stone-300 px-12 py-5 text-xs tracking-[0.3em] uppercase hover:bg-stone-50 transition-all duration-300 w-full md:w-auto">
              Live Activations
              <MousePointer2 className="w-3 h-3 transition-transform group-hover:translate-x-1 group-hover:-translate-y-1" />
            </button>
          </div>
        </div>

        <div className="absolute bottom-10 left-1/2 -translate-x-1/2 flex flex-col items-center gap-4 opacity-30 reveal-on-scroll">
          <span className="text-[8px] tracking-[0.5em] uppercase font-bold rotate-90 mb-8">Scroll</span>
          <div className="w-[1px] h-16 bg-gradient-to-b from-[#1A1A1A] to-transparent"></div>
        </div>
      </header>

      {/* Value Pillars */}
      <section className="py-20 bg-white border-y border-stone-100">
        <div className="max-w-7xl mx-auto px-6 grid grid-cols-1 md:grid-cols-3 gap-12 text-center">
          {[
            { icon: <Timer className="w-5 h-5" />, label: "Bespoke in 60s", desc: "Rapid Luxury Personalization" },
            { icon: <ShieldCheck className="w-5 h-5" />, label: "Boutique Ready", desc: "Corporate Standard Discipline" },
            { icon: <Sparkles className="w-5 h-5" />, label: "Sensory Stain", desc: "100% Organic Signature Mix" }
          ].map((item, i) => (
            <div key={i} className="reveal-on-scroll stagger-1 flex flex-col items-center group">
              <div className="w-12 h-12 rounded-full border border-stone-100 flex items-center justify-center mb-6 text-[#D4AF37] group-hover:bg-[#D4AF37] group-hover:text-white transition-all duration-500">
                {item.icon}
              </div>
              <h4 className="text-[10px] tracking-[0.2em] uppercase font-bold mb-2">{item.label}</h4>
              <p className="text-xs text-stone-400 uppercase tracking-widest">{item.desc}</p>
            </div>
          ))}
        </div>
      </section>

      {/* Services Section */}
      <section id="theedit" className="py-32 md:py-48">
        <div className="max-w-7xl mx-auto px-6">
          <div className="flex flex-col items-center mb-24 reveal-on-scroll">
            <span className="text-[10px] tracking-[0.5em] uppercase text-[#D4AF37] font-bold mb-4">Service Menu</span>
            <h3 className="text-4xl md:text-5xl font-serif text-center">The Artist's Edit</h3>
          </div>
          
          <div className="grid md:grid-cols-3 gap-10">
            {services.map((service, idx) => (
              <div key={idx} className={`reveal-on-scroll stagger-${idx+1} group relative p-12 bg-white border border-stone-100 transition-all duration-700 hover:shadow-2xl hover:shadow-stone-200/50 hover:-translate-y-4`}>
                <div className="absolute top-0 right-0 p-6 opacity-5 group-hover:opacity-20 transition-opacity">
                  {React.cloneElement(service.icon, { className: "w-24 h-24" })}
                </div>
                
                <div className="relative z-10">
                  <span className="inline-block px-2 py-1 bg-stone-50 text-[8px] tracking-[0.2em] uppercase font-bold mb-8 text-stone-400">
                    {service.tag}
                  </span>
                  <div className="text-[#D4AF37] mb-6 transform transition-transform duration-500 group-hover:scale-110 origin-left">
                    {service.icon}
                  </div>
                  <h4 className="text-[9px] tracking-[0.3em] uppercase mb-2 opacity-50 font-bold">{service.subtitle}</h4>
                  <h5 className="text-2xl font-serif mb-6 group-hover:text-[#D4AF37] transition-colors">{service.title}</h5>
                  <p className="text-sm text-stone-500 leading-relaxed mb-10 font-light">
                    {service.description}
                  </p>
                  <button className="text-[10px] tracking-[0.3em] uppercase font-bold flex items-center gap-2 group/btn">
                    Details <ArrowDown className="w-3 h-3 -rotate-45 group-hover/btn:rotate-0 transition-transform" />
                  </button>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Narrative Section - ARTIST PORTRAIT PLACEHOLDER HERE */}
      <section id="theartist" className="py-32 md:py-60 bg-[#1A1A1A] text-white relative overflow-hidden">
        <div className="absolute top-0 left-0 w-full h-full flex items-center justify-center opacity-[0.02] select-none pointer-events-none">
          <span className="text-[30vw] font-serif uppercase tracking-tighter">Bespoke</span>
        </div>

        <div className="max-w-7xl mx-auto px-6 relative z-10">
          <div className="grid md:grid-cols-2 gap-24 items-center">
            <div className="reveal-on-scroll">
              <div className="relative group">
                <div className="aspect-[4/5] bg-stone-800 rounded-sm overflow-hidden grayscale hover:grayscale-0 transition-all duration-1000">
                  {/* ARTIST PORTRAIT: Replace the src with your high-res professional portrait */}
                  <img 
                    src="https://i.imgur.com/FxBgYKY.png" 
                    alt="Rishana - Luxury Henna Artist Dubai" 
                    className="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity"
                  />
                  {/* Fallback label if image fails to load */}
                  <div className="absolute inset-0 flex items-center justify-center text-stone-600 text-[8px] tracking-widest uppercase pointer-events-none">
                    Artist Portrait
                  </div>
                </div>
                <div className="absolute -bottom-10 -right-10 w-72 p-10 bg-[#D4AF37] text-white hidden md:block shadow-2xl transition-transform group-hover:translate-x-2 group-hover:-translate-y-2 duration-500">
                  <p className="text-lg font-serif italic mb-4">"A gift is a story. I help your clients write theirs."</p>
                  <div className="w-8 h-[1px] bg-white opacity-40"></div>
                </div>
              </div>
            </div>
            
            <div className="space-y-12">
              <div className="reveal-on-scroll">
                <span className="text-[10px] tracking-[0.5em] uppercase text-[#D4AF37] font-bold mb-6 block">The Narrative</span>
                <h3 className="text-4xl md:text-6xl font-serif leading-[1.1]">The Corporate Mind, <br /><span className="italic text-stone-400">The Artistic Soul.</span></h3>
              </div>
              
              <div className="space-y-6 text-stone-400 text-lg font-light leading-relaxed reveal-on-scroll stagger-1">
                <p>
                  Rishana bridges the gap between high-level project management and delicate artistic expression. Every stroke is calculated; every design is a curated response to the brand identity.
                </p>
                <p>
                  Specializing in high-pressure, high-reward live activations—from luxury boutiques in Dubai Mall to exclusive bridal assemblies across the UAE.
                </p>
              </div>

              <div className="pt-8 reveal-on-scroll stagger-2">
                <button className="flex items-center gap-4 text-xs tracking-[0.4em] uppercase font-bold group">
                  <span className="w-12 h-[1px] bg-[#D4AF37] group-hover:w-20 transition-all duration-500"></span>
                  Read the Biography
                </button>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Contact & CTA */}
      <section id="contact" className="py-32 bg-[#FDFBF7]">
        <div className="max-w-4xl mx-auto px-6 text-center">
          <div className="reveal-on-scroll">
            <h3 className="text-4xl md:text-7xl font-serif mb-12">Let's Create <br /><span className="italic text-stone-400">Together.</span></h3>
          </div>
          
          <div className="grid md:grid-cols-3 gap-8 mb-20 reveal-on-scroll stagger-1">
            <div className="p-8 border border-stone-200 hover:border-[#D4AF37] transition-colors group cursor-pointer">
              <Mail className="w-5 h-5 mx-auto mb-4 opacity-40 group-hover:text-[#D4AF37] group-hover:opacity-100" />
              <span className="text-[10px] tracking-widest uppercase font-bold">Inquire</span>
            </div>
            <div className="p-8 border border-stone-200 hover:border-[#D4AF37] transition-colors group cursor-pointer">
              <Instagram className="w-5 h-5 mx-auto mb-4 opacity-40 group-hover:text-[#D4AF37] group-hover:opacity-100" />
              <span className="text-[10px] tracking-widest uppercase font-bold">Portfolio</span>
            </div>
            <div className="p-8 border border-stone-200 hover:border-[#D4AF37] transition-colors group cursor-pointer">
              <Phone className="w-5 h-5 mx-auto mb-4 opacity-40 group-hover:text-[#D4AF37] group-hover:opacity-100" />
              <span className="text-[10px] tracking-widest uppercase font-bold">WhatsApp</span>
            </div>
          </div>

          <form className="space-y-8 reveal-on-scroll stagger-2">
             <div className="grid md:grid-cols-2 gap-8">
              <div className="relative">
                <input type="text" className="w-full bg-transparent border-b border-stone-300 py-4 text-xs tracking-widest uppercase focus:outline-none focus:border-[#D4AF37] peer transition-colors" placeholder=" " />
                <label className="absolute left-0 top-4 text-[9px] tracking-widest uppercase font-bold text-stone-400 pointer-events-none transition-all peer-focus:-translate-y-6 peer-focus:text-[#D4AF37]">Name</label>
              </div>
              <div className="relative">
                <input type="email" className="w-full bg-transparent border-b border-stone-300 py-4 text-xs tracking-widest uppercase focus:outline-none focus:border-[#D4AF37] peer transition-colors" placeholder=" " />
                <label className="absolute left-0 top-4 text-[9px] tracking-widest uppercase font-bold text-stone-400 pointer-events-none transition-all peer-focus:-translate-y-6 peer-focus:text-[#D4AF37]">Email</label>
              </div>
            </div>
            <button className="w-full py-6 bg-[#1A1A1A] text-white text-[10px] tracking-[0.4em] uppercase font-bold hover:bg-[#D4AF37] transition-all duration-500 hover:shadow-2xl hover:shadow-[#D4AF37]/20">
              Submit Inquiry
            </button>
          </form>
        </div>
      </section>

      {/* Footer */}
      <footer className="py-20 bg-white border-t border-stone-100">
        <div className="max-w-7xl mx-auto px-6 flex flex-col items-center">
          <div className="flex flex-col items-center mb-12">
            <span className="text-2xl font-serif tracking-[0.3em] uppercase">Get Stained</span>
            <span className="text-[8px] tracking-[0.6em] uppercase opacity-40 mt-2">by Rishana</span>
          </div>
          
          <div className="flex gap-12 text-[9px] tracking-[0.3em] uppercase font-bold opacity-40 mb-12">
            <a href="#" className="hover:text-[#D4AF37] transition-colors">Privacy</a>
            <a href="#" className="hover:text-[#D4AF37] transition-colors">Terms</a>
            <a href="#" className="hover:text-[#D4AF37] transition-colors">Sitemap</a>
          </div>

          <p className="text-[10px] tracking-[0.1em] text-stone-400 uppercase">
            © 2026 Rishana Artist Studio. Handcrafted in Dubai.
          </p>
        </div>
      </footer>

      {/* Mobile Nav Overlay */}
      {isMenuOpen && (
        <div className="fixed inset-0 z-[200] bg-[#FDFBF7] p-8 flex flex-col justify-between animate-in slide-in-from-right duration-500">
          <div className="flex justify-between items-center">
            <span className="text-xl font-serif uppercase tracking-widest">Menu</span>
            <button onClick={() => setIsMenuOpen(false)} className="p-4"><X /></button>
          </div>
          <div className="flex flex-col gap-12 text-5xl font-serif">
            {['The Artist', 'The Edit', 'Portfolio', 'Contact'].map((item, i) => (
              <a key={i} href={`#${item.toLowerCase().replace(' ', '')}`} onClick={() => setIsMenuOpen(false)} className="hover:italic hover:text-[#D4AF37] transition-all">
                {item}
              </a>
            ))}
          </div>
          <div className="flex justify-between items-center text-[10px] tracking-widest uppercase font-bold opacity-40">
            <span>Dubai, UAE</span>
            <Instagram className="w-4 h-4" />
          </div>
        </div>
      )}
    </div>
  );
};

export default App;
